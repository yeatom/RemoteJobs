/* miniprogram/pages/job-detail/index.wxss */
.page {
  height: 100vh;
  background-color: var(--bg-page);
  display: flex;
  flex-direction: column;
  position: relative;
}

.detail-content {
  flex: 1;
  height: 0;
}

.detail-body {
  padding: 24rpx 43rpx;
  padding-bottom: 24rpx;
}

.collect-button {
  display: flex;
  align-items: flex-start;
  justify-content: center;
  padding: 0;
  min-width: 60rpx;
  flex-shrink: 0;
  margin-top: 10rpx;
}

.collect-icon-wrapper {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 6rpx;
}

.collect-icon {
  width: 76rpx;
  height: 76rpx;
  display: block;
  flex-shrink: 0;
}

.collect-text {
  font-size: 20rpx;
  color: var(--text-sub);
  line-height: 1;
}

.collect-text-active {
  color: var(--primary-dark);
}

.detail-header-section {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 24rpx;
  margin-top: 5rpx;
  margin-bottom: 24rpx;
}

.detail-title-meta-wrapper {
  flex: 1;
  min-width: 0;
  max-width: calc(100% - 84rpx - 20rpx); /* 收藏按钮宽度60rpx + gap 24rpx + 换行阈值20rpx */
}

.detail-title-row {
  margin-bottom: 19rpx;
}

.detail-title {
  font-size: 40rpx;
  font-weight: 600;
  color: var(--text-main);
  display: block;
  line-height: 1.2;
  overflow-wrap: break-word;
  word-break: break-word;
}

.detail-meta {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  font-size: 26rpx;
  color: var(--text-sub);
  line-height: 1.4;
}

.detail-meta-item {
  margin-right: 6rpx;
}

.detail-meta-dot {
  margin: 0 6rpx;
}

.detail-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-bottom: 25rpx;
}

.detail-tag {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 0 16rpx;
  height: 48rpx;
  border-radius: 999rpx;
  background-color: var(--primary-light);
}

.detail-tag-text {
  font-size: 24rpx;
  color: var(--primary-dark);
}


.detail-description-wrapper {
  margin-bottom: 200rpx; /* 为底部控制栏留出足够空间 */
}

.detail-description {
  padding: 32rpx;
  background-color: var(--bg-card);
  border-radius: 28rpx; /* 增大圆角以匹配简历卡片风格 */
  border: 1rpx solid rgb(226 232 240 / 60%);
  box-shadow: 0 8rpx 25rpx rgb(15 23 42 / 10%), 
              0 4rpx 8rpx rgb(15 23 42 / 3%);
}

.detail-description-rich {
  font-size: 27rpx;
  color: var(--text-main);
  line-height: 1.84;
  user-select: text;
  user-select: text;
}

.loading, .error {
  padding: 80rpx 32rpx;
  text-align: center;
  font-size: 28rpx;
  color: var(--text-holder);
}

/* 底部操作栏 (Dock) */
.apply-overlay {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  z-index: 1000;
  /* 蒙版高度独立设置 */
  height: calc(230rpx + env(safe-area-inset-bottom, 0)); 
  background: linear-gradient(to top, var(--bg-card) 0%, rgba(255, 255, 255, 0) 100%);
  pointer-events: none;
}

@media (prefers-color-scheme: dark) {
  .apply-overlay {
    background: linear-gradient(to top, var(--bg-card) 0%, rgba(30, 31, 32, 0) 100%);
  }
}

.apply-dock-content {
  position: absolute;
  left: 0;
  right: 0;
  /* 按钮到底部的留白独立设置 */
  bottom: calc(5rpx + env(safe-area-inset-bottom, 0));
  display: flex;
  align-items: center;
  justify-content: center;
  pointer-events: auto; /* 恢复按钮区域的点击 */
}

.apply-button {
  width: 45%; /* 按钮缩小10% (从50%到45%) */
  height: 83rpx; /* 按钮缩小10% (从92rpx到83rpx) */
  background: linear-gradient(135deg, #60a5fa 0%, #2563eb 100%); /* 浅蓝至深蓝渐变 */
  color: var(--bg-card);
  border-radius: 42rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  box-shadow: 0 10rpx 25rpx rgb(37 99 235 / 20%);
  transition: all 0.2s ease;
}

.apply-button-active {
  transform: scale(0.96);
  opacity: 0.95;
}

.apply-icon {
  width: 32rpx;
  height: 32rpx;
}

.apply-text {
  font-size: 30rpx;
  font-weight: 600;
  letter-spacing: 1rpx;
}

/* 功能抽屉 (Bottom Sheet) 局部调整 */
.apply-menu-scroll {
  max-height: 70vh;
  padding-bottom: 20rpx;
}

.apply-menu-content-padding {
  padding: 0 40rpx;
}

.apply-menu-section {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
  margin-bottom: 30rpx;
}

.apply-menu-item {
  height: 110rpx;
  background: var(--bg-page);
  border-radius: 24rpx;
  padding: 0 32rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  transition: all 0.2s ease;
}

.apply-menu-item:active {
  background: var(--border);
  transform: scale(0.99);
}

.apply-menu-item-left {
  display: flex;
  align-items: center;
  gap: 24rpx;
}

.apply-menu-item-text {
  font-size: 28rpx;
  font-weight: 500;
  color: var(--text-main);
}

.apply-menu-icon-placeholder {
  font-size: 32rpx;
  width: 48rpx;
  text-align: center;
}

.apply-menu-item-icon {
  width: 48rpx;
  height: 48rpx;
}

/* AI 样式 */
.ai-section {
  background: var(--border); /* 更中性的背景色 */
  padding: 24rpx;
  border-radius: 32rpx;
  border: 1rpx solid rgb(15 23 42 / 5%);
}

.ai-item {
  background: var(--bg-card) !important;
  border: 1rpx solid rgb(15 23 42 / 5%);
  box-shadow: 0 4rpx 12rpx rgb(0 0 0 / 2%);
}

.ai-text {
  color: #0f172a; /* 使用深藏青色，不再用亮蓝色 */
  font-weight: 600;
}

.ai-badge {
  display: none;
}

.apply-menu-safe-bottom {
  height: env(safe-area-inset-bottom, 0);
  margin-top: 10rpx;
}



