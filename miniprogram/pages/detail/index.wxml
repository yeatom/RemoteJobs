<!--miniprogram/pages/detail/index.wxml-->
<view class="container">
  <block wx:if="{{job}}">
    <text class="job-title">{{job.title}}</text>
    <!-- More job details will be added here later -->
  </block>
  <block wx:else>
    <text>{{loadingText}}</text>
  </block>
</view>

